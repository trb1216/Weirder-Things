import React from "react";
// import "../api/PostAPI";
import { userRegister } from "../api/RegisterAPI";

const Register = () => {
  return (
    <button type="button">
      register
    </button>
  );
};

export default Register;
