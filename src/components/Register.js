import React from "react";

// import { userRegister } from "../api/RegisterAPI";

const Register = () => {
  return (
    <button type="button">
      register
    </button>
  );
};

export default Register;
